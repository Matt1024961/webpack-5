import { expect } from 'chai';
import { Logger, LoggerManager } from 'typescript-logger';
//import { Navbar } from '.';


describe(`Navbar`, () => {
  // let logger: Logger;
  //let navbar: Navbar;
  // beforeEach(() => {
  //   logger = LoggerManager.create(`Inline XBRL Viewer`);
  //   //navbar = new Navbar(`#navbar`, logger);
  // });
  // it(`Creates Navbar`, () => {
  //   expect(navbar).to.not.be.false;
  // });

  // describe(`Navbar Renders`, () => {
  //   it(`Renders Navbar`, () => {
  //     expect(`#navbar .navbar`).to.exist;
  //   });

  //   it(`Renders Menu Button`, () => {
  //     expect(`#navbar .navbar [title="Menu"]`).to.exist;

  //     describe(`Navbar => Menu => Dropdown Renders`, () => {
  //       it(`Menu Button Dropdown renders`, () => {
  //         const button = document.querySelector(`.nav-link`) as HTMLElement;
  //         if (button) {
  //           button.click();
  //           expect(`#menu-dropdown`).to.exist;
  //         } else {
  //           expect(1).to.not.be.false;
  //         }
  //         // $(`#navbar .navbar [title="Menu"]`).trigger(`click`);
  //       });
  //     });
  //   });
  // });
});
