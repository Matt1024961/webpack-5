import { Error } from './';
describe('Error Component', () => {
  let component: Error;
  it('Renders header', () => {
    console.log(component);
    expect(1).toEqual(1);
  });
  it('Renders', () => {
    expect(2).toEqual(2);
  });
});
