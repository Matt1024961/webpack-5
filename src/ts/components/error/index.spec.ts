import { Error } from './';
describe('Error Component', () => {
  console.log(Error);
  it('Renders header', () => {
    expect(1).toEqual(1);
  });
  it('Renders', () => {
    expect(2).toEqual(2);
  });
});
